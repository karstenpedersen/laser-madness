/// @description 

draw_rectangle(bbox_left, bbox_top, bbox_right, bbox_bottom, true);

/*
// Draw sprite stacked
if (distance_to_object(cnt_camera) <= global.camera_width) {
	draw_sprite_stacked();
}